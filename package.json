{
  "name": "bloodyowl-upgrade-reason-react",
  "version": "0.1.0",
  "description": "An alternative upgrade script to ReasonReact 0.7.0",
  "license": "MIT",
  "esy": {
    "build": "refmterr dune build -p #{self.name}",
    "NOTE": "Optional release Section. Customizes result of `esy release`",
    "release": {
      "bin": [
        "Upgrade"
      ]
    },
    "install": [
      "esy-installer bloodyowl-upgrade-reason-react.install",
      "esy-installer bloodyowl-upgrade-reason-react-tests.install"
    ]
  },
  "scripts": {
    "test": "esy x RunTests.exe",
    "doc": "esy dune build @doc"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@reason-native/console": "*",
    "@reason-native/pastel": "*",
    "@reason-native/rely": "*",
    "@esy-ocaml/reason": ">= 3.4.0 < 3.5.0",
    "@opam/reason": ">= 3.4.0 < 3.5.0",
    "refmterr": "*",
    "ocaml": "~4.6.0"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "ocaml": "~4.6.0",
    "@opam/odoc": "*"
  }
}
